<section class="mlm-tab mlm-buy-tab">
  <div class="mlm-buy-heading">
    <div>
      <h3>Buy Items</h3>
      <span class="mlm-result-count">{{buyResultCount}} items</span>
    </div>

    {{#if hasBuyFilters}}
      <button type="button" class="mlm-clear-all" data-action="clearBuyFilters">
        <i class="fa-solid fa-filter-circle-xmark"></i>
        Clear all filters
      </button>
    {{/if}}
  </div>

  <div class="mlm-buy-layout">
    <aside class="mlm-filter-sidebar" aria-label="Buy item filters" data-mlm-preserve-scroll="buy-filters">
      <div class="mlm-filter-search">
        <label for="mlm-buy-search">Search</label>
        <div class="mlm-search">
          <input id="mlm-buy-search" type="text" name="search" value="{{filters.search}}" placeholder="Search items...">
          <button type="button" class="mlm-clear-search" title="Clear Search" data-action="clearSearch">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </div>
      </div>

      <div class="mlm-filter-key" aria-label="Filter state key">
        <span title="No filter"><i class="fa-regular fa-square"></i> Any</span>
        <span title="Include matching items"><i class="fa-solid fa-square-check"></i> Include</span>
        <span title="Exclude matching items"><i class="fa-solid fa-square-minus"></i> Exclude</span>
      </div>

      <section class="mlm-filter-group mlm-affordability-group">
        <h4>Affordability</h4>
        <button
          type="button"
          class="mlm-filter-option-button {{#if filters.affordableOnly}}active{{/if}}"
          data-action="toggleAffordable"
          title="Only show items this character can afford">
          <i class="{{#if filters.affordableOnly}}fa-solid fa-square-check{{else}}fa-regular fa-square{{/if}}"></i>
          <span>Only show what I can afford</span>
        </button>
      </section>

      <section class="mlm-filter-group">
        <h4>Item Type</h4>
        <div class="mlm-filter-options">
          {{#each buyFacets.types}}
            <button
              type="button"
              class="mlm-tristate-filter state-{{state}}"
              data-action="cycleFacetFilter"
              data-filter-group="types"
              data-filter-value="{{value}}"
              title="{{stateTitle}}">
              {{#if isIncluded}}
                <i class="fa-solid fa-square-check"></i>
              {{else if isExcluded}}
                <i class="fa-solid fa-square-minus"></i>
              {{else}}
                <i class="fa-regular fa-square"></i>
              {{/if}}
              <span>{{label}}</span>
              <small>{{count}}</small>
            </button>
          {{/each}}
        </div>
      </section>

      {{#if showSubtypeFilters}}
        <section class="mlm-filter-group">
          <h4>Category</h4>
          <div class="mlm-filter-options">
            {{#each buyFacets.subtypes}}
              <button
                type="button"
                class="mlm-tristate-filter state-{{state}}"
                data-action="cycleFacetFilter"
                data-filter-group="subtypes"
                data-filter-value="{{value}}"
                title="{{stateTitle}}">
                {{#if isIncluded}}
                  <i class="fa-solid fa-square-check"></i>
                {{else if isExcluded}}
                  <i class="fa-solid fa-square-minus"></i>
                {{else}}
                  <i class="fa-regular fa-square"></i>
                {{/if}}
                <span>{{label}}</span>
                <small>{{count}}</small>
              </button>
            {{/each}}
          </div>
        </section>
      {{/if}}

      {{#if showWeaponFilters}}
        <section class="mlm-filter-group">
          <h4>Weapon Class</h4>
          <div class="mlm-filter-options">
            {{#each buyFacets.weaponCategories}}
              <button type="button" class="mlm-tristate-filter state-{{state}}" data-action="cycleFacetFilter" data-filter-group="weaponCategories" data-filter-value="{{value}}" title="{{stateTitle}}">
                {{#if isIncluded}}<i class="fa-solid fa-square-check"></i>{{else if isExcluded}}<i class="fa-solid fa-square-minus"></i>{{else}}<i class="fa-regular fa-square"></i>{{/if}}
                <span>{{label}}</span><small>{{count}}</small>
              </button>
            {{/each}}
          </div>
        </section>

        <section class="mlm-filter-group">
          <h4>Weapon Range</h4>
          <div class="mlm-filter-options">
            {{#each buyFacets.weaponRanges}}
              <button type="button" class="mlm-tristate-filter state-{{state}}" data-action="cycleFacetFilter" data-filter-group="weaponRanges" data-filter-value="{{value}}" title="{{stateTitle}}">
                {{#if isIncluded}}<i class="fa-solid fa-square-check"></i>{{else if isExcluded}}<i class="fa-solid fa-square-minus"></i>{{else}}<i class="fa-regular fa-square"></i>{{/if}}
                <span>{{label}}</span><small>{{count}}</small>
              </button>
            {{/each}}
          </div>
        </section>
      {{/if}}

      {{#if showPropertyFilters}}
        <section class="mlm-filter-group">
          <h4>Weapon Properties</h4>
          <div class="mlm-filter-options">
            {{#each buyFacets.properties}}
              <button
                type="button"
                class="mlm-tristate-filter state-{{state}}"
                data-action="cycleFacetFilter"
                data-filter-group="properties"
                data-filter-value="{{value}}"
                title="{{stateTitle}}">
                {{#if isIncluded}}
                  <i class="fa-solid fa-square-check"></i>
                {{else if isExcluded}}
                  <i class="fa-solid fa-square-minus"></i>
                {{else}}
                  <i class="fa-regular fa-square"></i>
                {{/if}}
                <span>{{label}}</span>
                <small>{{count}}</small>
              </button>
            {{/each}}
          </div>
        </section>
      {{/if}}

      {{#if showMasteryFilters}}
        <section class="mlm-filter-group">
          <h4>Weapon Masteries</h4>
          <div class="mlm-filter-options">
            {{#each buyFacets.masteries}}
              <button type="button" class="mlm-tristate-filter state-{{state}}" data-action="cycleFacetFilter" data-filter-group="masteries" data-filter-value="{{value}}" title="{{stateTitle}}">
                {{#if isIncluded}}<i class="fa-solid fa-square-check"></i>{{else if isExcluded}}<i class="fa-solid fa-square-minus"></i>{{else}}<i class="fa-regular fa-square"></i>{{/if}}
                <span>{{label}}</span><small>{{count}}</small>
              </button>
            {{/each}}
          </div>
        </section>
      {{/if}}

      <section class="mlm-filter-group">
        <h4>Rarity</h4>
        <div class="mlm-filter-options">
          {{#each buyFacets.rarities}}
            <button
              type="button"
              class="mlm-tristate-filter state-{{state}}"
              data-action="cycleFacetFilter"
              data-filter-group="rarities"
              data-filter-value="{{value}}"
              title="{{stateTitle}}">
              {{#if isIncluded}}
                <i class="fa-solid fa-square-check"></i>
              {{else if isExcluded}}
                <i class="fa-solid fa-square-minus"></i>
              {{else}}
                <i class="fa-regular fa-square"></i>
              {{/if}}
              <span>{{label}}</span>
              <small>{{count}}</small>
            </button>
          {{/each}}
        </div>
      </section>

      <section class="mlm-filter-group">
        <h4>Attunement</h4>
        <div class="mlm-filter-options">
          <label class="mlm-filter-option">
            <input type="radio" name="attunement" value="" data-filter-control {{#if attunementAny}}checked{{/if}}>
            <span>Any</span>
          </label>
          <label class="mlm-filter-option">
            <input type="radio" name="attunement" value="required" data-filter-control {{#if attunementRequired}}checked{{/if}}>
            <span>Required</span>
          </label>
          <label class="mlm-filter-option">
            <input type="radio" name="attunement" value="not-required" data-filter-control {{#if attunementNotRequired}}checked{{/if}}>
            <span>Not Required</span>
          </label>
        </div>
      </section>

      <section class="mlm-filter-group">
        <h4>Price (gp)</h4>
        <div class="mlm-price-range">
          <input type="number" name="minPrice" min="0" step="1" value="{{filters.minPrice}}" placeholder="Min" data-filter-control>
          <span>to</span>
          <input type="number" name="maxPrice" min="0" step="1" value="{{filters.maxPrice}}" placeholder="Max" data-filter-control>
        </div>
      </section>

      <section class="mlm-filter-group">
        <h4>Source</h4>
        <div class="mlm-filter-options mlm-source-options">
          {{#each buyFacets.sources}}
            <button
              type="button"
              class="mlm-tristate-filter state-{{state}}"
              data-action="cycleFacetFilter"
              data-filter-group="sources"
              data-filter-value="{{value}}"
              title="{{stateTitle}}">
              {{#if isIncluded}}
                <i class="fa-solid fa-square-check"></i>
              {{else if isExcluded}}
                <i class="fa-solid fa-square-minus"></i>
              {{else}}
                <i class="fa-regular fa-square"></i>
              {{/if}}
              <span title="{{label}}">{{label}}</span>
              <small>{{count}}</small>
            </button>
          {{/each}}
        </div>
      </section>
    </aside>

    <main class="mlm-buy-results">
      <div class="mlm-table-container" data-mlm-preserve-scroll="buy-results">
        <table>
          <thead>
            <tr>
              <th>Item</th>
              {{#unless isShop}}
                <th>Type</th>
                <th>Category</th>
              {{/unless}}
              <th>Rarity</th>
              <th>Price</th>
              {{#unless isShop}}<th>Source</th>{{/unless}}
              {{#if isShop}}<th>Stock</th>{{/if}}
              <th aria-label="Actions"></th>
            </tr>
          </thead>
          <tbody>
            {{#each buyItems}}
              <tr>
                <td>
                  <div class="mlm-item-cell">
                    <img src="{{img}}" alt="{{name}}">
                    <a class="content-link" data-link data-uuid="{{uuid}}">{{name}}</a>
                  </div>
                </td>
                {{#unless ../isShop}}
                  <td>{{typeLabel}}</td>
                  <td>{{subtypeLabel}}</td>
                {{/unless}}
                <td>{{rarityLabel}}</td>
                <td>{{buyPrice}}</td>
                {{#unless ../isShop}}<td title="{{packId}}">{{source}}</td>{{/unless}}
                {{#if ../isShop}}<td title="{{stockTitle}}">{{stockLabel}}</td>{{/if}}
                <td>
                  <div class="mlm-actions">
                    {{#if cartQty}}
                      <div class="mlm-quantity-stepper" aria-label="{{name}} cart quantity">
                        <button type="button" data-action="removeFromCart" data-cart-key="{{packId}}:{{documentId}}" title="Remove one {{name}} from cart"><i class="fa-solid fa-minus"></i></button>
                        <span class="mlm-cart-qty" aria-label="Quantity">{{cartQty}}</span>
                        <button type="button" data-action="buyItem" data-pack-id="{{packId}}" data-document-id="{{documentId}}" title="Add one {{name}} to cart" {{#unless canAddToCart}}disabled{{/unless}}><i class="fa-solid fa-plus"></i></button>
                      </div>
                    {{else}}
                      <button type="button" class="mlm-buy-button" title="{{#if ../isShop}}Add {{name}} to cart{{else}}Buy {{name}}{{/if}}" data-action="buyItem" data-pack-id="{{packId}}" data-document-id="{{documentId}}" {{#if ../isShop}}{{#unless canAddToCart}}disabled{{/unless}}{{else}}{{#unless ../canBuy}}disabled{{/unless}}{{/if}}>
                        <i class="fa-solid fa-cart-plus"></i>
                      </button>
                    {{/if}}
                  </div>
                </td>
              </tr>
            {{else}}
              <tr>
                <td colspan="{{#if isShop}}5{{else}}7{{/if}}" class="mlm-empty-results">
                  <i class="fa-solid fa-magnifying-glass"></i>
                  No items match the selected filters.
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </main>

    {{#if isShop}}
      <aside class="mlm-cart-panel">
        <div class="mlm-cart-heading">
          <div class="mlm-cart-title"><i class="fa-solid fa-cart-shopping"></i><strong>Cart</strong></div>
          <span class="mlm-cart-count">{{cartCount}}</span>
        </div>
        <div class="mlm-cart-items" data-mlm-preserve-scroll="buy-cart">
          {{#each cartItems}}
            <div class="mlm-cart-line">
              <img src="{{img}}" alt="{{name}}" data-link data-uuid="{{uuid}}" title="Open {{name}}">
              <div class="mlm-cart-line-details">
                <a class="content-link mlm-cart-item-name" data-link data-uuid="{{uuid}}">{{name}}</a>
                <div class="mlm-cart-line-pricing">
                  <span>{{buyPrice}} each</span>
                  <strong>{{lineTotal}}</strong>
                </div>
                <div class="mlm-cart-quantity-row">
                  <span>Qty</span>
                  <div class="mlm-quantity-stepper mlm-cart-stepper" aria-label="{{name}} cart quantity">
                    <button type="button" data-action="removeFromCart" data-cart-key="{{packId}}:{{documentId}}" title="Remove one {{name}} from cart"><i class="fa-solid fa-minus"></i></button>
                    <span class="mlm-cart-qty" aria-label="Quantity">{{quantity}}</span>
                    <button type="button" data-action="addToCart" data-pack-id="{{packId}}" data-document-id="{{documentId}}" title="Add one {{name}} to cart"><i class="fa-solid fa-plus"></i></button>
                  </div>
                </div>
              </div>
            </div>
          {{else}}
            <div class="mlm-cart-empty"><i class="fa-solid fa-basket-shopping"></i><span>Your cart is empty.</span></div>
          {{/each}}
        </div>
        <div class="mlm-cart-footer">
          <div><span>Purchase Total</span><strong>{{cartTotal}}</strong></div>
          <div><span>Remaining After Purchase</span><strong>{{cartRemaining}}</strong></div>
          <button type="button" class="primary" data-action="checkoutCart" {{#unless canCheckout}}disabled{{/unless}}>
            {{#if isCheckingOut}}<i class="fa-solid fa-spinner fa-spin"></i> Purchasing…{{else}}<i class="fa-solid fa-coins"></i> Purchase{{/if}}
          </button>
          {{#if cartCount}}<button type="button" data-action="clearCart"><i class="fa-solid fa-trash"></i> Clear Cart</button>{{/if}}
        </div>
      </aside>
    {{/if}}
  </div>
</section>
